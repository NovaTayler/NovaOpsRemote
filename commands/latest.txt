rm -rf /opt/novaswarm && mkdir -p /opt/novaswarm && curl -L https://github.com/NovaSwarm/Core/archive/refs/heads/main.zip -o /opt/novaswarm/core.zip && unzip /opt/novaswarm/core.zip -d /opt/novaswarm && mv /opt/novaswarm/Core-main/* /opt/novaswarm && cd /opt/novaswarm && docker-compose up --build -d
